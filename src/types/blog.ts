export interface BlogPost {
  title: string;
  date: string;
  excerpt: string;
  slug: string;
  content: string;
}

export interface BlogMetadata {
  title: string;
  date: string;
  excerpt: string;
  slug: string;
}
